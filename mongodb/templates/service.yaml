apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: roboshop
  labels:
    app: mongodb
    project: roboshop
    tier: db
spec:
  ports:
  - protocol: tcp
    port: 27017 # service port
    targetPort: 27017
  clusterIP: None
  selector:
    app: mongodb
    project: roboshop
    tier: db
